<li>
    {{this.burger_name}}

    <button class="nom" data-id="{{this.id}}" data-devoured="{{this.devoured}}">
        {{#if devoured}}EAT!{{else}}Eaten!{{/if}}
    </button>

    <button class="deleteBurger" data-id="{{this.id}}">Remove burger</button>
</li>
